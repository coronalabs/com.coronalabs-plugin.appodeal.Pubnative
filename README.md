Sources for the plugin `plugin.appodeal.Pubnative`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Pubnative" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
